# SecureUSB-By-ayoub-2025
SecureUSB is a lightweight, client-side web utility for encrypting and decrypting files on a USB drive using the Web Crypto API. It features an in-browser file encryptor and decryptor (no server required) that derives an AES-256 key from a user-provided password using PBKDF2.
